int main() {
    int integerValue = sizeof(int); // integer in bytes
    int shortValue = sizeof(short); // short in bytes
    int longValue = sizeof(long); // long in bytes
    int charValue = sizeof(char); // char in bytes
    int floatValue = sizeof(float); // float in bytes
    int doubleValue = sizeof(double); // double in bytes
    return 0;
}
